version: "2"

run:
  timeout: 3m

linters:
  enable:
    - govet # Официальный линтер от Go Team.
    - staticcheck # Статический анализатор кода.
    - ineffassign # Находит неиспользуемые переменные.
    - goconst # Находит повторяющиеся литералы.

issues:
  exclude-use-default: false
